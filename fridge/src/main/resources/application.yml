#spring.application.name=fridge (estava aqui quando era application.properties)

spring:
  datasource:
    url: jdbc:h2:testedb
    driver-class-name: org.h1.Driver
    username: ThEsposito
    password:

    h2:
      console:
        enabled: true
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: validate
      show-sql: true
    flyway:
      enabled: true